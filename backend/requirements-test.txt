# Lightweight, deterministic dependencies for API contract tests.
# The RAG boundary is stubbed in tests/test_integration.py; production dependency
# compatibility is validated separately by the Docker build and pip-audit.
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32
email-validator==2.3.0
sqlalchemy==2.0.52
psycopg2-binary==2.9.9
redis==5.0.1
PyJWT[crypto]==2.13.0
bcrypt==4.1.2
httpx==0.28.1
# Last green CI resolution: AnyIO 4.15 deprecates the alias used by Starlette
# 1.6 TestClient and breaks our warnings-as-errors gate during collection.
anyio==4.14.2
pgvector==0.5.0
beautifulsoup4==4.12.3
pytest==8.4.2
